language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t phananhdev/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true phananhdev/docker-react yarn run test
